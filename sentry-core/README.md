# Sentry Haskell SDK: `sentry-core`

This package provides the core of the Sentry SDK for integration and third-party library authors that want to instrument their code.

Regular users who wish to integrate Sentry into their applications should use the `sentry` package, which provides a default transport implementation and support for common third-party libraries.
