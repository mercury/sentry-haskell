# Sentry Haskell SDK: `sentry-core`

This package provides the core of the Sentry SDK, primarily intended for integration and third-party library authors that want to instrument their code.

Regular users who wish to integrate Sentry into their applications should use the `sentry` package, which is a "batteries included" library that provides a default transport implementation and support for common third-party libraries.
