-- Inherit packages, the patrol source-repository-package, constraints, and
-- allow-newer from the base project so this file can't drift out of sync.
import: cabal.project

optimization: 2

profiling: True

-- Insert cost centres after optimization (-fprof-late) so the .prof attributes
-- time/allocation accurately without perturbing the optimizer.
profiling-detail: late-toplevel
